<!DOCTYPE html>
<html>
<head>
  <title>Lista de Usuarios</title>
</head>
<body>
  <h1>Usuarios Registrados</h1>

  <ul>
    <% if (stars.length === 0) { %>
      <li>No hay usuarios</li>
    <% } else { %>
      <% stars.forEach(stars => { %>
        <li><%= stars.name %></li>
      <% }) %>
    <% } %>
  </ul>
</body>
</html>
